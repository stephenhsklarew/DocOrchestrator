# Qwilo Gmail Pipeline - Example Configuration
# Copy this file to qwilo_gmail_pipeline.yaml and customize for your setup

name: "Qwilo Gmail Pipeline"

# Global settings
global:
  mode: "test" # Using Qwen 2.5 32B (local, free)

# Stage 1: Idea Generation
idea_generation:
  source: "gmail"
  start_date: "01012025" # January 1, 2025 - MMDDYYYY format
  label: "qwilo-test" # Gmail label to filter
  email_subject: "" # Email subject filter (optional)
  focus: "AI strategy and innovation"
  fast_mode: false # false = Qwen 2.5 32B (local, ~11 tok/s), true = Gemini 2.5 Flash (cloud, 300+ tok/s)
  combined_topics: false # Save each topic separately
  select_all: true # Auto-select all emails for scheduled/automated runs

# Stage 2: Document Generation
document_generation:
  # Writing configuration
  style: "" # Using default style
  audience: "business leaders"
  type: "blog post"
  size: "2000 words"

  # Optional customer story
  customer_story: "" # AI will create fictional example

  # Output location
  output: "./output" # Local directory

# Orchestration settings
orchestration:
  stage1_timeout: 1800 # 30 minutes for idea generation (first run processes many emails)
  stage2_timeout: 300 # 5 minutes per document
  retry_on_failure: true
  save_session: true
  log_level: "INFO" # INFO level logging
  use_manifest: false # Manifest mode not yet fully implemented
  batch_mode: true # Batch mode for automated/scheduled runs
